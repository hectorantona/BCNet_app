<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bcnet_app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="string"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BCNet_app">
        <activity android:name=".FormNewProfilePictureActivity"></activity>
        <activity android:name=".SearchMapaActivity" />

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="${MAPS_API_KEY}" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".FormNewEmail" />
        <activity android:name=".FormNewPswd" />
        <activity android:name=".NewLocalActivity" />
        <activity android:name=".PerfilActivity" />
        <activity android:name=".FormCommentCovid" />
        <activity android:name=".FormValoracio" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity2" />
        <activity
            android:name=".SignInActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewLocalitzacio" />
    </application>

</manifest>